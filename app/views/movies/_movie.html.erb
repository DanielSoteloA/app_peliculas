<div id="<%= dom_id movie %>" class="card" style="width: 28rem;">
  <div class="card-body">
    <h5 class="card-title">Película</h5>
    <h6 class="card-subtitle mb-2 text-muted"><%= movie.Name %></h6>
    <p class="card-text"><%= movie.Synopsis %></p>
    <h5 class="card-title">Director</h5>
    <h6 class="card-subtitle mb-2 text-muted"><%= movie.Director %></h6>
    <%= link_to "Editar", edit_movie_path(@movie), :class => "btn btn-primary" %>
    <%= link_to "Volver al Listado", root_path, :class => "btn btn-secondary" %>
  </div>
</div>
